- Operation cost currently uses the operation's manual duration.
  Supporting the *based on work orders* duration mode is a possible
  enhancement.
- A "what-if" cost simulation (recompute the rollup against a
  hypothetical set of component prices, without writing it back) is
  planned as a separate module, `mrp_bom_cost_simulation`.
- Optional scheduled action to refresh `standard_price` from BoMs in
  bulk.
