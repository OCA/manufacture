<?xml version="1.0" encoding="utf-8" ?>
<odoo>

<record id="action_show_input_line" model="ir.actions.act_window">
    <field name="name">Input line information</field>
    <field name="res_model">input.line</field>
    <field name="view_mode">form</field>
    <field name="target">new</field>
</record>

<record id="input_line_form_window" model="ir.ui.view">
    <field name="name">input.line.form.window</field>
    <field name="model">input.line</field>
    <field name="mode">primary</field>
    <field name="arch" type="xml">
        <form>
            <sheet>
                <h1>
                    <field name="name" class="oe_inline" placeholder="Name" />
                </h1>
                <group name="main">
                </group>
            </sheet>
        </form>
    </field>
</record>

<record id='input_line_tree' model='ir.ui.view'>
    <field name='model'>input.line</field>
    <field name='arch' type='xml'>
        <tree editable="bottom" multi_edit="1" class="input_line_tree">
            <field name="configured_bom_id" invisible="1" />
            <field name="sequence" widget="handle" />
            <button name="ui_clone" class="fa fa-clone" type="object" title="Clone" />
            <button
                    name="%(action_show_input_line)d"
                    class="fa fa-eye"
                    type="action"
                    title="Info"
                />
            <button
                    name="action_show_configured_bom"
                    class="fa fa-cog"
                    type="object"
                    title="Info"
                    attrs="{'invisible': [('configured_bom_id', '=', False)]}"
                />
            <field name="name" />
            <field name="count" />
            <field name="alert" optional="show" />
            <field name="comment" optional="show" />
            <field name="checked" optional="hide" />
            <field name="config_id" invisible="1" />
        </tree>
    </field>
</record>

<record id="input_line_search" model="ir.ui.view">
    <field name="model">input.line</field>
    <field name="arch" type="xml">
        <search>
            <filter name="name" />
            <filter
                    name="config_id"
                    string="Config"
                    domain="[]"
                    context="{'group_by' : 'config_id'}"
                />
            <filter
                    name="bom_id"
                    string="Bom"
                    domain="[]"
                    context="{'group_by' : 'bom_id'}"
                />
        </search>
    </field>
</record>

</odoo>
