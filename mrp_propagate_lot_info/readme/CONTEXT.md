Some manufacturing flows need produced lots to keep information from the lot
consumed for a key component.

For example, a component lot can define expiration or traceability metadata that
must remain available on the finished product lot, or on selected by-product
lots, after production.

The same need can appear on manual manufacturing orders that do not use a BoM.
In that case, the manufactured product category can define which propagation
profile should be used.
