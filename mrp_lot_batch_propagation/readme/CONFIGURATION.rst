Go to Products > Product Categories and set "MRP Batch Propagate" on categories that contain batch products.
Alternatively, go to Products > Products and set "MRP Batch Propagate" on individual products.

When creating Manufacturing Orders for batch products, the system will automatically:
- Store the origin BOM on produced lots
- Display consumed batch origin BOMs on subsequent manufacturing operations
