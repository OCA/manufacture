<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_production_internal_search" model="ir.ui.view">
        <field name="name">mrp.production.internal.search</field>
        <field name="model">mrp.production</field>
        <field name="inherit_id" ref="mrp.view_mrp_production_filter" />
        <field name="arch" type="xml">
            <filter name="activities_exception" position="after">
                <separator />
                <filter
                    name="needs_review"
                    string="Needs my Review"
                    domain="[('reviewer_ids', 'in', uid), ('state', 'not in', ['done', 'cancel'])]"
                    help="Production to review"
                />
                <filter
                    name="tier_validated"
                    string="Validated"
                    domain="[('validated', '=', True)]"
                    help="Production validated and ready to be done"
                />
            </filter>
        </field>
    </record>
</odoo>
