<?xml version="1.0" encoding="utf-8" ?>
<odoo>
        <!-- Extend BoM form view to add batch size fields -->
        <record id="mrp_bom_form_view_inherit_batch_size" model="ir.ui.view">
            <field name="name">mrp.bom.form.inherit.batch_size</field>
            <field name="model">mrp.bom</field>
            <field name="inherit_id" ref="mrp.mrp_bom_form_view" />
            <field name="arch" type="xml">
                <xpath expr="//field[@name='consumption']" position="after">
                    <label for="batch_size" invisible="type != 'normal'" />
                    <div class="o_row" invisible="type != 'normal'">
                        <field name="enable_batch_size" />
                        <field
                        name="batch_size"
                        class="w-50"
                        invisible="not enable_batch_size"
                        decoration-danger="batch_size &lt;= 0.0"
                    />
                        <field
                        name="product_uom_id"
                        groups="uom.group_uom"
                        readonly="1"
                        invisible="not enable_batch_size"
                    />
                    </div>
                </xpath>
            </field>
        </record>
</odoo>
