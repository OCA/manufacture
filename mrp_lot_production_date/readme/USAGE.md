To get production dates automatically set on produced lots you need to
configure your products to be tracked by lots or serial numbers.

Then at the validation of a manufacturing order the production date will
be set automatically on the produced lot.

When a production date is set on a lot (either automatically through MRP or manually), the system automatically calculates all expiration-related dates.
This feature ensures accurate expiration tracking based on actual production dates rather than lot creation dates.
