This module can be used to determine the applicability of a component in a
production order based on a formula defined in the BOM that depends on factors
such as the product, BOM, or the production order itself.

